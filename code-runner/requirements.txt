# Dependencies for the code-runner HTTP service itself.
# (The data-science packages live in ../environment/requirements.txt and are
#  installed into the SAME environment so the kernel can import them.)
fastapi
uvicorn[standard]
pydantic>=2
jupyter_client
ipykernel
