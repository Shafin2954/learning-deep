# ============================================================================
# learning-deep — full data-science / machine-learning environment
# This is THE environment the code-runner executes against, so every package
# listed here is importable from any "Try it Yourself" block on the site.
#
# Install (CPU, works everywhere):
#     python -m venv .venv && source .venv/bin/activate    # Win: .venv\Scripts\activate
#     pip install -U pip
#     pip install -r requirements.txt
#
# GPU — install torch from the CUDA index FIRST, then this file:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#     pip install -r requirements.txt
#
# Lock exact versions once it works:  pip freeze > requirements.lock.txt
# ============================================================================

# ---- core numeric / dataframes --------------------------------------------
numpy
pandas
scipy
polars            # fast dataframe — worth learning alongside pandas
pyarrow           # parquet, arrow-backed pandas

# ---- visualization --------------------------------------------------------
matplotlib
seaborn
plotly

# ---- classical ML / stats -------------------------------------------------
scikit-learn
statsmodels
pingouin          # clean statistical tests (ties to your stats depth)

# ---- gradient boosting (your Kaggle stack) --------------------------------
xgboost
lightgbm
catboost

# ---- imbalance, encoding, feature work ------------------------------------
imbalanced-learn
category_encoders

# ---- tuning + interpretability --------------------------------------------
optuna
shap

# ---- dimensionality reduction / clustering --------------------------------
umap-learn
hdbscan

# ---- deep learning --------------------------------------------------------
# CPU torch by default. For GPU, install from the CUDA index (see header) before
# running this file, then pip will keep your CUDA build.
torch
torchvision

# ---- NLP / LLMs (ties to Fixeth + your RAG work) --------------------------
transformers
tokenizers
datasets
sentence-transformers
accelerate

# ---- kernel (required by the code-runner) ---------------------------------
ipykernel
jupyter_client

# ---- optional: TensorFlow / Keras -----------------------------------------
# Heavy and sometimes conflicts with torch's CUDA libs. Uncomment only if you
# specifically need TF examples. torch alone covers all deep-learning chapters.
# tensorflow
# keras
